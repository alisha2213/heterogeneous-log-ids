# heterogeneous-log-ids
Hybrid intrusion detection system fusing network features and log text via TF-IDF, compressed through a deep autoencoder, classified with a stacked ensemble (Random Forest, XGBoost, Logistic Regression).
